rm ('servers.txt');
var spi = false;
while (fileExists ('servers.txt') == false) {
  exec ('spider.script');
}

rm ('Bankserver.txt');

var spi = false;
while (fileExists ('Bankserver.txt') == false) {
  exec ('target.script');
}

exec ('buy.script');
exec ('propag.script');
